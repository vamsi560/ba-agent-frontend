# Requify Frontend

This is the React + Vite frontend for Requify Agent Pro.

## Commands

### Development
```bash
npm install
npm run dev
```

### Build for Production
```bash
npm run build
```

The build assets will be generated in the `dist` folder. The backend is configured to serve these assets automatically if the `dist` folder is present.
